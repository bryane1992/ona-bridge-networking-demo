{
  "name": "Bridge Networking Demo (with workaround)",
  "image": "mcr.microsoft.com/devcontainers/universal:5.1.5",
  "forwardPorts": [8080, 8081],
  "postStartCommand": "bash .devcontainer/fix-iptables.sh && docker compose up -d --build"
}
